//
// Created by jorda on 12/3/2025.
//

#ifndef HONORARYBUDGETAPP_CATEGORY_H
#define HONORARYBUDGETAPP_CATEGORY_H

#include <iostream>

#include "budgetmanager.h"

struct ExpenseCategory {
    QString name;
    double amount;
};

class Category {
    public:

    /*
    vector<ExpenseCategory> categories = {
        {"Gas", 0},
        {"Food", 0},
        {"Utilities", 0},
        {"Bills", 0}
    };

    void addCategoryAmount(const vector<Expense> expenses) {

        for (int i = 0; i < expenses.size(); i++)
            {
            if (expenses[i].name == QString("Gas"))
                {categories[1].amount += expenses[i].amount;}

            }
        }


    };
    */
};

#endif //HONORARYBUDGETAPP_CATEGORY_H